apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  to:
    kind: Service
    name: {{ .Values.name }}
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None