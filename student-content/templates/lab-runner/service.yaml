---
apiVersion: v1
kind: Service
metadata:
  name: lab-runner
  namespace: ai501
  labels:
    app.kubernetes.io/name: lab-runner
    app.kubernetes.io/instance: lab-runner
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: lab-runner
    app.kubernetes.io/instance: lab-runner
