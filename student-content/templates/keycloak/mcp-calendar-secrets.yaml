---
# OAuth Client Secret for MCP Calendar Server
apiVersion: v1
kind: Secret
metadata:
  name: mcp-oauth-secret
  namespace: keycloak
  labels:
    app: mcp-calendar
    component: oauth
  annotations:
    description: "OAuth client secret for MCP Calendar Server"
type: Opaque
stringData:
  # This is the client secret for token introspection
  # Exported from: tests/helm-mcp-auth/keycloak/mcp-configuration-export.json
  client-secret: "zrqR06T0NBTct1ZP08SK8NAc8JfVRW0n"

---
# Shared user password for all MCP Calendar realm users
# Used by: testuser, user1, user2, ... userN
apiVersion: v1
kind: Secret
metadata:
  name: mcp-user-password-secret
  namespace: keycloak
  labels:
    app: mcp-calendar
    component: oauth
  annotations:
    description: "Shared password for all MCP Calendar OAuth realm users"
type: Opaque
stringData:
  # Shared password for all users in the MCP Calendar realm
  password: "thisisthepassword"
