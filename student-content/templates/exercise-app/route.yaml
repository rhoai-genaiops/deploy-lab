---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: exercise-app
spec:
  path: /
  to:
    kind: Service
    name: exercise-app
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
