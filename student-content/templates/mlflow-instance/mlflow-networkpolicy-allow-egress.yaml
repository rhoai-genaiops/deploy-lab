---
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: mlflow-allow-egress
  namespace: redhat-ods-applications
  labels:
    app: mlflow
    component: mlflow
spec:
  podSelector:
    matchLabels:
      app: mlflow
  egress:
    - {}
  policyTypes:
    - Egress
