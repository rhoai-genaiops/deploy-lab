apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "canopy-board.fullname" . }}
  labels:
    {{- include "canopy-board.labels" . | nindent 4 }}
data:
  NODE_ENV: "production"
  PORT: "3000"
  DB_PATH: "/app/data/canopy-credits.db"
  LOG_LEVEL: "info"
  CORS_ORIGINS: "https://canopy-credits.example.com"
