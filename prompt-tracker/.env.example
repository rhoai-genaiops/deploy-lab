# Git Repository Configuration
# If not provided, will use current directory as the repository

# Git repository URL (required for external repositories)
GIT_REPO_URL=https://github.com/your-username/your-repo.git

# Git authentication credentials
GIT_USERNAME=your-username
GIT_PASSWORD=your-password-or-token

# Git branch to monitor (optional, defaults to 'main')
GIT_BRANCH=main

# Monitoring interval in seconds (optional, defaults to 30)
MONITOR_INTERVAL=30

# Flask configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=true