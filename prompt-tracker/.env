# Git repository URL (required for external repositories)
GIT_REPO_URL=https://gitea-gitea.apps.cluster-gm86c.gm86c.sandbox1062.opentlc.com/user1/canopy-be.git

# Git authentication credentials
GIT_USERNAME=user1
GIT_PASSWORD=thisisthepassword

# Git branch to monitor (optional, defaults to 'main')
GIT_BRANCH=main

# Monitoring interval in seconds (optional, defaults to 30)
MONITOR_INTERVAL=30

# Flask configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5001
FLASK_DEBUG=true